export default  (theme) => {
    return {
      container: {
        width: '40%',
        margin: '10% auto',
        padding: theme.spacing.unit * 2
      },
      textField: {
        marginBottom: theme.spacing.unit * 2
      }
    }
  }